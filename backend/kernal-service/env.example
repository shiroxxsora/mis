# На сервере UTF8 (server_encoding/client_encoding). Базы kernal нет — укажите одну из существующих.
# Пример списка с локальной машины: postgres, erad, sfr_init, sfr_nsi
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
